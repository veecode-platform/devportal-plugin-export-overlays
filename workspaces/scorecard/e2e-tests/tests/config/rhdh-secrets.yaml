apiVersion: v1
kind: Secret
metadata:
  name: rhdh-secrets
type: Opaque
stringData:
  GITHUB_URL: https://github.com
  GITHUB_ORG: janus-qe
  GITHUB_ORG_2: janus-test
  JIRA_TOKEN: $VAULT_JIRA_TOKEN
  GITHUB_APP_APP_ID: $VAULT_GITHUB_OVERLAYS_APP_ID
  GITHUB_APP_CLIENT_ID: $VAULT_GITHUB_OVERLAYS_APP_CLIENT_ID
  GITHUB_APP_CLIENT_SECRET: $VAULT_GITHUB_OVERLAYS_APP_CLIENT_SECRET
  GITHUB_APP_WEBHOOK_URL: $VAULT_GITHUB_OVERLAYS_APP_WEBHOOK_URL
  GITHUB_APP_WEBHOOK_SECRET: $VAULT_GITHUB_OVERLAYS_APP_WEBHOOK_SECRET
  GITHUB_APP_PRIVATE_KEY: $VAULT_GITHUB_OVERLAYS_APP_PRIVATE_KEY
  GITLAB_HOST: "gitlab.com"
  GITLAB_TOKEN: $VAULT_GITLAB_TOKEN_DECODED
  JIRA_URL: https://redhat.atlassian.net
