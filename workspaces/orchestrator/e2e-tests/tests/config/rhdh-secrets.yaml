apiVersion: v1
kind: Secret
metadata:
  name: rhdh-secrets
type: Opaque
stringData:
  SONATAFLOW_DATA_INDEX_URL: $SONATAFLOW_DATA_INDEX_URL
  LOKI_BASE_URL: http://localhost:3100
  # Required by orchestrator-backend-module-loki at startup; e2e does not deploy Loki.
  AUTH_TOKEN: e2e-ci-placeholder
