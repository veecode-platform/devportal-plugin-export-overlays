apiVersion: v1
kind: Secret
metadata:
  name: rhdh-secrets
type: Opaque
stringData:
  VAULT_EVENTS_GITLAB_HOST: $VAULT_EVENTS_GITLAB_HOST
  VAULT_EVENTS_GITLAB_TOKEN: $VAULT_EVENTS_GITLAB_TOKEN
  VAULT_EVENTS_GITLAB_PARENT_ORG: $VAULT_EVENTS_GITLAB_PARENT_ORG
  VAULT_GITLAB_WEBHOOK_SECRET: $VAULT_GITLAB_WEBHOOK_SECRET
